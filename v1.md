# v1.0 — 多数据源配置与动态切换

## 1. 版本目标

解决"如何连接并管理多个数据库"的问题。

此版本没有任何分片逻辑，只做一件事：从 YAML 配置读取多个数据源，用 Spring 的 `AbstractRoutingDataSource` 统一管理，通过一个 key 手动切换到目标数据源。这是整个分库分表框架的地基。

---

## 2. 核心功能说明

### 类比

> 把多个数据库想象成多个仓库（ds0、ds1）。`AbstractRoutingDataSource` 是仓库管理员，手里拿着一张地图（Map），你告诉他"去 ds0"，他就把你带到 ds0 的大门口。

### 流程图

```
application.yml
  spring.sharding.datasources:
    ds0: url/username/password
    ds1: url/username/password
        │
        ▼
ShardingProperties (读取配置，封装成 Map)
        │
        ▼
ShardingAutoConfiguration (创建 Bean，构造连接池 Map)
        │
        ▼
ShardingDataSource extends AbstractRoutingDataSource
        │
        ▼
determineCurrentLookupKey()
        │  此版本：返回硬编码的 "ds0" 或 "ds1"
        ▼
路由到对应物理数据库连接
```

### 关键机制

`AbstractRoutingDataSource` 是 Spring 提供的抽象类，内部维护一个 `Map<Object, DataSource>`。每次获取连接时，它调用 `determineCurrentLookupKey()` 拿到 key，再从 Map 中找到对应的真实 DataSource。

---

## 3. 代码结构

```
src/main/java/com/example/masharding/
├── config/
│   ├── ShardingProperties.java        # 读取 YAML 配置
│   └── ShardingAutoConfiguration.java # 注册 Spring Bean
└── datasource/
    └── ShardingDataSource.java        # 继承 AbstractRoutingDataSource
```

```
src/main/resources/
└── application.yml                    # 数据源配置
```

---

## 4. 代码实现说明

### ShardingProperties.java

读取 `spring.sharding.datasources` 下的多数据源配置，每个数据源包含 url、username、password、driverClassName。

```java
@ConfigurationProperties(prefix = "spring.sharding")
@Component
public class ShardingProperties {

    // key = "ds0" / "ds1"，value = 数据源配置项
    private Map<String, DataSourceProperties> datasources = new LinkedHashMap<>();

    public Map<String, DataSourceProperties> getDatasources() {
        return datasources;
    }

    public void setDatasources(Map<String, DataSourceProperties> datasources) {
        this.datasources = datasources;
    }

    public static class DataSourceProperties {
        private String url;
        private String username;
        private String password;
        private String driverClassName = "com.mysql.cj.jdbc.Driver";
        // getter / setter 省略
    }
}
```

### ShardingDataSource.java

继承 `AbstractRoutingDataSource`，重写 `determineCurrentLookupKey()`。v1.0 中直接返回硬编码的 key，后续版本会改为从 ThreadLocal 读取。

```java
public class ShardingDataSource extends AbstractRoutingDataSource {

    // v1.0 硬编码，仅用于验证路由机制
    private String currentKey = "ds0";

    public void setCurrentKey(String key) {
        this.currentKey = key;
    }

    @Override
    protected Object determineCurrentLookupKey() {
        System.out.println("determineCurrentLookupKey = " + currentKey);
        return currentKey;
    }
}
```

### ShardingAutoConfiguration.java

负责读取配置、创建 Druid 连接池、注册 `ShardingDataSource` Bean。

```java
@Configuration
@EnableConfigurationProperties(ShardingProperties.class)
public class ShardingAutoConfiguration {

    @Bean
    public ShardingDataSource shardingDataSource(ShardingProperties properties) {
        Map<Object, Object> targetDataSources = new LinkedHashMap<>();

        for (Map.Entry<String, ShardingProperties.DataSourceProperties> entry
                : properties.getDatasources().entrySet()) {
            String dsName = entry.getKey();
            ShardingProperties.DataSourceProperties dsProps = entry.getValue();

            DruidDataSource druid = new DruidDataSource();
            druid.setUrl(dsProps.getUrl());
            druid.setUsername(dsProps.getUsername());
            druid.setPassword(dsProps.getPassword());
            druid.setDriverClassName(dsProps.getDriverClassName());

            targetDataSources.put(dsName, druid);
        }

        ShardingDataSource shardingDataSource = new ShardingDataSource();
        shardingDataSource.setTargetDataSources(targetDataSources);
        // 设置默认数据源（找不到 key 时的兜底）
        shardingDataSource.setDefaultTargetDataSource(
            targetDataSources.values().iterator().next()
        );
        return shardingDataSource;
    }
}
```

### application.yml

```yaml
spring:
  sharding:
    datasources:
      ds0:
        url: jdbc:mysql://localhost:3306/db0?useSSL=false&serverTimezone=UTC
        username: root
        password: root
      ds1:
        url: jdbc:mysql://localhost:3306/db1?useSSL=false&serverTimezone=UTC
        username: root
        password: root
```

### ApplicationRunner 验证代码

```java
@Component
public class TestRunner implements ApplicationRunner {

    @Autowired
    private ShardingDataSource shardingDataSource;

    @Autowired
    private JdbcTemplate jdbcTemplate;

    @Override
    public void run(ApplicationArguments args) throws Exception {
        // 切换到 ds0
        shardingDataSource.setCurrentKey("ds0");
        List<Map<String, Object>> result0 = jdbcTemplate.queryForList("select * from test");
        System.out.println("ds0 result: " + result0);

        // 切换到 ds1
        shardingDataSource.setCurrentKey("ds1");
        List<Map<String, Object>> result1 = jdbcTemplate.queryForList("select * from test");
        System.out.println("ds1 result: " + result1);
    }
}
```

---

## 5. 测试流程

### 准备数据库

```sql
-- 在 db0 执行
CREATE TABLE test (id INT, name VARCHAR(50));
INSERT INTO test VALUES (1, 'from-db0');

-- 在 db1 执行
CREATE TABLE test (id INT, name VARCHAR(50));
INSERT INTO test VALUES (2, 'from-db1');
```

### 启动并观察日志

```bash
mvn spring-boot:run
```

预期控制台输出：

```
determineCurrentLookupKey = ds0
ds0 result: [{id=1, name=from-db0}]

determineCurrentLookupKey = ds1
ds1 result: [{id=2, name=from-db1}]
```

### curl 验证（若暴露 HTTP 接口）

```bash
# 查询 ds0 数据
curl -X GET "http://localhost:8080/test?ds=ds0"

# 查询 ds1 数据
curl -X GET "http://localhost:8080/test?ds=ds1"
```

---

## 6. 版本总结

### 学到了什么

- `AbstractRoutingDataSource` 的路由机制：每次 `getConnection()` 时调用 `determineCurrentLookupKey()`，实现运行时动态切换数据源。
- Spring `@ConfigurationProperties` 可以将 YAML 中的 Map 结构直接绑定到 Java 对象。
- Druid 连接池的编程式创建方式。

### 与前版本对比

| 项目 | v1.0 |
|------|------|
| 数据源数量 | 多个（可配置） |
| 路由方式 | 硬编码 key |
| 分片逻辑 | 无 |
| 线程安全 | 否（currentKey 是实例变量） |

### 局限性

- `currentKey` 是实例变量，多线程下会互相覆盖，不能用于生产。
- 路由 key 需要手动设置，没有自动计算逻辑。
- 没有 SQL 改写能力，逻辑表名和物理表名必须一致。

### 设计要点

`AbstractRoutingDataSource` 是整个框架的核心枢纽，后续所有版本都围绕"如何正确地给 `determineCurrentLookupKey()` 提供值"来演进。v1.0 用硬编码，v2.0 改为 ThreadLocal，v3.0~v6.0 逐步实现自动计算。