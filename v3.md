# v3.0 — 分片策略与 Groovy 表达式引擎

## 1. 版本目标

解决"如何根据分片列的值自动计算目标库/表"的问题。

实现 `ShardingStrategy` 接口和 `HashShardingStrategy`，用 Groovy 动态执行 `ds${id % 2}` 这类内联表达式，根据分片列的值计算出目标库名或目标表名。此版本不依赖数据库，可纯单元测试验证。

---

## 2. 核心功能说明

### 类比

> 制定"分拣规则"——快递单号末位是偶数去 A 仓库，奇数去 B 仓库。规则写成公式（`ds${id % 2}`），随时可换，不需要重新编译代码。

### Groovy 内联表达式执行原理

```
配置: algorithmExpression = "ds${id % 2}"
      shardingColumn = "id"
        │
        ▼
InlineExpressionParser
  步骤1: handlePlaceHolder
         "ds${id % 2}"  →  "ds${it.id % 2}"
         （把变量名包装成 closure 参数访问形式）
        │
        ▼
  步骤2: evaluateClosure
         GroovyShell.evaluate("{it -> ds${it.id % 2}}")
         → 得到一个 Groovy Closure 对象
        │
        ▼
  步骤3: 绑定参数并执行
         closure.setProperty("id", 3)
         closure.call()  →  "ds1"
```

### 分片策略接口设计

```
ShardingStrategy (接口)
    │
    └── HashShardingStrategy (实现)
            │
            ├── shardingColumn: "id"
            ├── algorithmExpression: "ds${id % 2}"
            │
            └── doSharding(
                    availableTargets = ["ds0", "ds1"],
                    logicTable = "user",
                    params = {"id": 3}
                ) → "ds1"
```

---

## 3. 代码结构

```
src/main/java/com/example/masharding/
├── config/
│   ├── ShardingProperties.java        # 新增 tableStrategy / databaseStrategy 配置
│   └── ShardingAutoConfiguration.java # 不变
├── context/
│   ├── ShardingResult.java            # 不变
│   └── ShardingContext.java           # 不变
├── datasource/
│   └── ShardingDataSource.java        # 不变
└── strategy/
    ├── ShardingStrategy.java          # 新增：策略接口
    ├── HashShardingStrategy.java      # 新增：哈希取模策略实现
    └── InlineExpressionParser.java    # 新增：Groovy 表达式解析器
```

---

## 4. 代码实现说明

### ShardingStrategy.java

策略接口，定义统一的分片计算方法。

```java
public interface ShardingStrategy {

    /**
     * 计算目标分片名称
     *
     * @param availableTargets 所有可用的目标名称列表（如 ["ds0","ds1"] 或 ["user0","user1","user2"]）
     * @param logicTable       逻辑表名（如 "user"）
     * @param params           分片参数 Map（如 {"id": 3}）
     * @return 目标名称（如 "ds1" 或 "user0"）
     */
    String doSharding(List<String> availableTargets, String logicTable, Map<String, Object> params);
}
```

### InlineExpressionParser.java

封装 GroovyShell，解析并缓存内联表达式，执行时绑定参数。

```java
public class InlineExpressionParser {

    // 脚本缓存，避免重复编译（v8.0 会改为 ConcurrentHashMap）
    private static final Map<String, Script> SCRIPTS = new HashMap<>();

    private final String expression;

    public InlineExpressionParser(String expression) {
        this.expression = expression;
    }

    /**
     * 将 "ds${id % 2}" 转换为 Groovy closure 字符串
     * 使得变量可以通过 closure 的 delegate 访问
     */
    private String handlePlaceHolder(String expr) {
        // 将 ${varName} 替换为 ${it.varName}，使变量通过 it 访问
        return expr.replaceAll("\\$\\{(\\w+)", "\\${it.$1");
    }

    /**
     * 执行表达式，传入参数 Map，返回计算结果字符串
     */
    public String evaluate(Map<String, Object> params) {
        String closureExpr = "{it -> \"" + handlePlaceHolder(expression) + "\"}";

        Script script = SCRIPTS.computeIfAbsent(closureExpr, key -> {
            GroovyShell shell = new GroovyShell();
            return shell.parse(key);
        });

        // 执行脚本得到 Closure
        Closure<?> closure = (Closure<?>) script.run();

        // 创建参数对象，将 Map 中的 key-value 设置为 closure 的属性
        GroovyObject delegate = new GroovyObjectSupport() {};
        for (Map.Entry<String, Object> entry : params.entrySet()) {
            delegate.setProperty(entry.getKey(), entry.getValue());
        }
        closure.setDelegate(delegate);
        closure.setResolveStrategy(Closure.DELEGATE_FIRST);

        return closure.call(delegate).toString();
    }
}
```

### HashShardingStrategy.java

读取配置中的 `shardingColumn` 和 `algorithmExpression`，调用 `InlineExpressionParser` 执行计算。

```java
public class HashShardingStrategy implements ShardingStrategy {

    // 分片列名，如 "id"
    private final String shardingColumn;

    // 内联表达式，如 "ds${id % 2}"
    private final String algorithmExpression;

    private final InlineExpressionParser parser;

    public HashShardingStrategy(String shardingColumn, String algorithmExpression) {
        this.shardingColumn = shardingColumn;
        this.algorithmExpression = algorithmExpression;
        this.parser = new InlineExpressionParser(algorithmExpression);
    }

    @Override
    public String doSharding(List<String> availableTargets,
                             String logicTable,
                             Map<String, Object> params) {
        // 从参数 Map 中取出分片列的值
        Object shardingValue = params.get(shardingColumn);
        if (shardingValue == null) {
            throw new IllegalArgumentException(
                "分片列 [" + shardingColumn + "] 的值不能为 null");
        }

        // 执行 Groovy 表达式计算目标名称
        String target = parser.evaluate(params);

        // 验证计算结果在可用目标列表中
        if (!availableTargets.contains(target)) {
            throw new IllegalStateException(
                "计算结果 [" + target + "] 不在可用目标列表 " + availableTargets + " 中");
        }

        return target;
    }
}
```

### ShardingProperties.java（新增策略配置）

```java
@ConfigurationProperties(prefix = "spring.sharding")
@Component
public class ShardingProperties {

    private Map<String, DataSourceProperties> datasources = new LinkedHashMap<>();

    // 分库策略配置
    private StrategyProperties databaseStrategy;

    // 分表策略配置（按逻辑表名区分）
    private Map<String, StrategyProperties> tableStrategies = new LinkedHashMap<>();

    // 各逻辑表对应的物理表列表
    private Map<String, List<String>> actualTables = new LinkedHashMap<>();

    public static class StrategyProperties {
        private String shardingColumn;
        private String algorithmExpression;
        // getter / setter 省略
    }
    // getter / setter 省略
}
```

### application.yml（新增策略配置）

```yaml
spring:
  sharding:
    datasources:
      ds0:
        url: jdbc:mysql://localhost:3306/db0?useSSL=false&serverTimezone=UTC
        username: root
        password: root
      ds1:
        url: jdbc:mysql://localhost:3306/db1?useSSL=false&serverTimezone=UTC
        username: root
        password: root
    database-strategy:
      sharding-column: id
      algorithm-expression: "ds${id % 2}"
    table-strategies:
      user:
        sharding-column: id
        algorithm-expression: "user${id % 3}"
    actual-tables:
      user: [user0, user1, user2]
```

---

## 5. 测试流程

### 单元测试（无需数据库）

```java
public class HashShardingStrategyTest {

    @Test
    public void testDatabaseSharding() {
        HashShardingStrategy strategy = new HashShardingStrategy("id", "ds${id % 2}");
        List<String> targets = Arrays.asList("ds0", "ds1");

        for (int id = 0; id <= 5; id++) {
            Map<String, Object> params = Collections.singletonMap("id", id);
            String result = strategy.doSharding(targets, "user", params);
            System.out.println("id=" + id + " → " + result);
        }
    }

    @Test
    public void testTableSharding() {
        HashShardingStrategy strategy = new HashShardingStrategy("id", "user${id % 3}");
        List<String> targets = Arrays.asList("user0", "user1", "user2");

        for (int id = 0; id <= 5; id++) {
            Map<String, Object> params = Collections.singletonMap("id", id);
            String result = strategy.doSharding(targets, "user", params);
            System.out.println("id=" + id + " → " + result);
        }
    }
}
```

### 预期输出

```
# 数据库分片（ds${id % 2}）
id=0 → ds0
id=1 → ds1
id=2 → ds0
id=3 → ds1
id=4 → ds0
id=5 → ds1

# 表分片（user${id % 3}）
id=0 → user0
id=1 → user1
id=2 → user2
id=3 → user0
id=4 → user1
id=5 → user2
```

### curl 验证（若暴露 HTTP 接口）

```bash
# 验证 id=3 路由到 ds1.user0
curl -X GET "http://localhost:8080/sharding/calc?id=3"
# 预期: {"db":"ds1","table":"user0"}

# 验证 id=4 路由到 ds0.user1
curl -X GET "http://localhost:8080/sharding/calc?id=4"
# 预期: {"db":"ds0","table":"user1"}
```

---

## 6. 版本总结

### 学到了什么

- Groovy 内联表达式（`ds${id % 2}`）是 ShardingSphere 的经典设计，用动态脚本替代硬编码的 if-else，让分片规则可配置化。
- `GroovyShell.parse()` 编译脚本开销较大，必须缓存 `Script` 对象，只编译一次。
- 策略接口 `ShardingStrategy` 的设计使得未来可以轻松扩展其他策略（范围分片、一致性哈希等）。

### 与前版本对比

| 项目 | v2.0 | v3.0 |
|------|------|------|
| 路由 key 来源 | 手动 set | 表达式自动计算 |
| 分片规则 | 无 | Groovy 内联表达式 |
| 可配置性 | 否 | 是（YAML 配置） |
| 依赖数据库 | 是 | 否（纯计算） |

### 局限性

- 还不能自动从 SQL 中提取分片列的值，仍需手动构造 `params` Map。
- `HashMap` 缓存在高并发下存在竞态条件（v8.0 修复）。
- 只支持精确值分片，不支持范围查询（v10.0 扩展）。

### 设计要点

`InlineExpressionParser` 将 Groovy 表达式转换为 Closure 的关键技巧是 `handlePlaceHolder`：把 `${id}` 改写为 `${it.id}`，使变量通过 closure 的 delegate 对象访问，从而实现参数动态绑定。这是整个表达式引擎的核心。
